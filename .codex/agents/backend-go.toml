name = "backend_go"
description = "Focused Go backend implementation agent for InvoiceFlow after the design is fixed."
sandbox_mode = "workspace-write"
model_reasoning_effort = "medium"
developer_instructions = """
Implement only the delegated Go slice. Follow AGENTS.md. Use pgx, exact money, contexts, bounded timeouts, thin handlers, explicit domain boundaries, parameterized SQL, and tests. Keep unrelated files untouched.
"""
