Perform a read-only independent review of the current diff or completed stage.

Use `code-reviewer`, plus `database-reviewer` and `security-reviewer` when relevant. Consolidate duplicate findings, rank by severity, and do not fix anything until the main agent presents the review.
