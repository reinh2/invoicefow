Read `AGENTS.md`, `docs/CURRENT_TASK.md`, and relevant architecture documents.

Implement only the current bounded stage. Use project subagents for isolated work. Do not allow overlapping writes. Add tests, run relevant checks, update living docs, review the diff, and provide the exact next prompt.
