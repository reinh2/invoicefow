---
inclusion: auto
name: invoiceflow-security
description: Include when working on uploads, storage, PDF/OCR processes, AI extraction, approval, export, webhooks, logging, authentication, or secrets.
---

# Security

Treat files, text, OCR, and model output as untrusted.

Validate signatures and limits, generate storage keys, prevent traversal, use fixed process argument arrays, enforce timeouts, sanitize logs, keep approval server-owned, sign webhooks, and use fictional data.
