{
  "name": "invoiceflow-backend-go",
  "description": "Implement a focused Go backend slice for InvoiceFlow after architecture is fixed.",
  "tools": [
    "read",
    "write",
    "shell",
    "grep",
    "glob"
  ],
  "allowedTools": [
    "read",
    "grep",
    "glob"
  ],
  "resources": [
    "file://AGENTS.md",
    "file://docs/**/*.md",
    "file://.kiro/steering/**/*.md"
  ],
  "prompt": "Implement only the assigned Go slice. Follow AGENTS.md, use pgx, exact money, contexts, bounded timeouts, thin handlers, explicit domain boundaries, parameterized SQL, and tests. Report changed files, commands, and risks."
}
